# 04. DynamoDB 테이블 생성

> Phase 3 | 소요 시간: 약 10분 | 선행 조건: [02. AWS IAM](02.aws-iam.md)
> 참고: [AWS DynamoDB Getting Started](https://docs.aws.amazon.com/amazondynamodb/latest/developerguide/getting-started-step-1.html)

---

## 4-1. 테이블 설계 요약

4개 테이블, 모두 **On-Demand** 용량 모드.

| 테이블명 | PK | GSI | 용도 |
|----------|-----|-----|------|
| `cas-contracts` | `id` (String) | — | 계약서 메타데이터 + 파싱 결과 |
| `cas-risk-reports` | `id` (String) | `contract_id-index` (PK: `contract_id`) | 리스크 리포트 |
| `cas-workflow-steps` | `id` (String) | `contract_id-index` (PK: `contract_id`) | 워크플로우 검토 단계 |
| `cas-prompt-templates` | `id` (String) | `contract_type-index` (PK: `contract_type`) | 프롬프트 템플릿 |

## 4-2. 테이블 생성 (콘솔 — 4회 반복)

1. AWS 콘솔 → `DynamoDB` 검색 → DynamoDB 서비스 클릭
2. 리전: **아시아 태평양 (서울) ap-northeast-2**
3. **테이블(Tables)** → **테이블 생성(Create table)**

| 필드 | 값 |
|------|-----|
| **테이블 이름** | `cas-contracts` (첫 번째) |
| **파티션 키** | `id` → **문자열(String)** |
| **정렬 키** | (비워둠) |

4. **설정 사용자 지정(Customize settings)** 선택 → **용량 모드**: **온디맨드(On-demand)**
5. **테이블 생성** 클릭

> **중요**: "기본 설정"을 선택하면 프로비저닝 모드가 됨. 반드시 **설정 사용자 지정**에서 On-Demand 선택.

## 4-3. GSI 추가

`cas-risk-reports`, `cas-workflow-steps`, `cas-prompt-templates` 3개 테이블에 GSI 추가.

테이블 생성 시 **설정 사용자 지정** → 아래 스크롤 → **보조 인덱스** → **글로벌 인덱스 생성**:

| 테이블 | GSI 파티션 키 | 인덱스 이름 | 프로젝션 |
|--------|-------------|------------|---------|
| cas-risk-reports | `contract_id` (String) | `contract_id-index` | ALL |
| cas-workflow-steps | `contract_id` (String) | `contract_id-index` | ALL |
| cas-prompt-templates | `contract_type` (String) | `contract_type-index` | ALL |

## 4-4. AWS CLI로 일괄 생성 (대안)

```bash
# 테이블 1: cas-contracts (GSI 없음)
aws dynamodb create-table \
  --table-name cas-contracts \
  --attribute-definitions AttributeName=id,AttributeType=S \
  --key-schema AttributeName=id,KeyType=HASH \
  --billing-mode PAY_PER_REQUEST \
  --region ap-northeast-2

# 테이블 2: cas-risk-reports (GSI: contract_id)
aws dynamodb create-table \
  --table-name cas-risk-reports \
  --attribute-definitions \
    AttributeName=id,AttributeType=S \
    AttributeName=contract_id,AttributeType=S \
  --key-schema AttributeName=id,KeyType=HASH \
  --global-secondary-indexes \
    '[{
      "IndexName": "contract_id-index",
      "KeySchema": [{"AttributeName":"contract_id","KeyType":"HASH"}],
      "Projection": {"ProjectionType":"ALL"}
    }]' \
  --billing-mode PAY_PER_REQUEST \
  --region ap-northeast-2

# 테이블 3: cas-workflow-steps (GSI: contract_id)
aws dynamodb create-table \
  --table-name cas-workflow-steps \
  --attribute-definitions \
    AttributeName=id,AttributeType=S \
    AttributeName=contract_id,AttributeType=S \
  --key-schema AttributeName=id,KeyType=HASH \
  --global-secondary-indexes \
    '[{
      "IndexName": "contract_id-index",
      "KeySchema": [{"AttributeName":"contract_id","KeyType":"HASH"}],
      "Projection": {"ProjectionType":"ALL"}
    }]' \
  --billing-mode PAY_PER_REQUEST \
  --region ap-northeast-2

# 테이블 4: cas-prompt-templates (GSI: contract_type)
aws dynamodb create-table \
  --table-name cas-prompt-templates \
  --attribute-definitions \
    AttributeName=id,AttributeType=S \
    AttributeName=contract_type,AttributeType=S \
  --key-schema AttributeName=id,KeyType=HASH \
  --global-secondary-indexes \
    '[{
      "IndexName": "contract_type-index",
      "KeySchema": [{"AttributeName":"contract_type","KeyType":"HASH"}],
      "Projection": {"ProjectionType":"ALL"}
    }]' \
  --billing-mode PAY_PER_REQUEST \
  --region ap-northeast-2
```

## 4-5. 테이블 생성 확인

```bash
aws dynamodb list-tables --region ap-northeast-2
# 4개 테이블 모두 "Active" 확인
```

## 4-6. 데이터 모델 상세 (아이템 구조)

**cas-contracts:**
```json
{
  "id": "contract-001",
  "customer_name": "A사",
  "contract_type": "SI",
  "version": 1,
  "status": "RISK_REVIEWED",
  "is_standard": false,
  "s3_key": "A사/contract-001/v1.docx",
  "uploaded_at": "2026-05-10T09:00:00Z",
  "uploaded_by": "영업팀 홍길동",
  "clauses_json": "[{\"id\":\"clause_001\",\"type\":\"liability\",...}]",
  "diff_report_json": null
}
```

**cas-risk-reports:**
```json
{
  "id": "risk-001",
  "contract_id": "contract-001",
  "overall_risk": "HIGH",
  "risk_summary": "3건의 고위험 조항 발견",
  "is_standard_contract": false,
  "escalation_required": true,
  "standard_deviation": "배상한도 미설정, CR 절차 누락",
  "key_concerns_json": "[\"무제한 배상책임\",\"CR 절차 미정의\",\"과도한 지체상금\"]",
  "created_at": "2026-05-10T09:01:00Z",
  "clause_risks_json": "[{\"clause_id\":\"clause_001\",\"risk_level\":\"HIGH\",\"risk_type\":\"무제한_배상책임\",\"reason\":\"배상한도 미설정\",\"recommendation\":\"계약금액 100% 이내로 제한\",\"financial_impact\":\"계약금액 초과 손실 가능\"}]"
}
```

**cas-workflow-steps:**
```json
{
  "id": "wf-001",
  "contract_id": "contract-001",
  "step_order": 1,
  "department": "계약팀",
  "assignee": "김계약",
  "status": "PENDING",
  "comment": null,
  "signed_at": null,
  "created_at": "2026-05-10T09:02:00Z"
}
```

**cas-prompt-templates:**
```json
{
  "id": "prompt-si-default",
  "contract_type": "SI",
  "prompt_name": "SI 도급 계약 리스크 분석",
  "system_prompt": "당신은 메가존클라우드의 계약검토 전문가입니다...",
  "updated_at": "2026-05-10T08:00:00Z",
  "updated_by": "admin"
}
```

---

**이전**: [03. S3 버킷](03.s3-bucket.md) | **다음**: [05. Bedrock 모델 액세스](05.bedrock-model-access.md)
